name: CI/CD Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Copy files to server
        run: |
          rsync -avz --delete ./ ubuntu@YOUR_SERVER_IP:/home/ubuntu/web/

      - name: Deploy with Docker Compose
        run: |
          ssh ubuntu@YOUR_SERVER_IP "cd /home/ubuntu/web && docker compose down && docker compose up -d --build"
