name: Deploy Web

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: SSH to server and deploy
        uses: appleboy/ssh-action@v0.1.8
        with:
          host: 46.34.165.222      # آی‌پی سرور
          username: root           # یوزر
          password: P@$$w0rd       # پسورد (می‌تونی secret هم بذاری)
          port: 22
          script: |
            cd /root/web
            git pull origin main
            docker compose down -v
            docker compose up -d --build
